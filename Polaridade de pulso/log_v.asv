function [v,naocoincidem,i_erros] = log_v(myiPeak,iPeak)
    v_erros = abs((myiPeak(:)-iPeak(:))./iPeak(:)*100);
    v = [myiPeak(:),iPeak(:), v_erros.*(v_erros>1)];
    n = 1:size(iPeak,1);
    i_erros = n(v(:,3)~=0);
end